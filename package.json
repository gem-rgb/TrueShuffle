{
  "name": "trueshuffle",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "node scripts/frontend.mjs dev",
    "build": "node scripts/frontend.mjs build",
    "typecheck": "tsc --noEmit",
    "preview": "vite preview",
    "backend:dev": "python -m uvicorn backend.app:app --reload --host 127.0.0.1 --port 8000",
    "backend:serve": "python -m uvicorn backend.app:app --host 127.0.0.1 --port 8000",
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build"
  },
  "dependencies": {
    "@tauri-apps/api": "^1.6.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^1.6.0",
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.1",
    "typescript": "^5.5.4",
    "vite": "^5.4.2"
  }
}

# chore(deps): upgrade React to version 18\n# chore(deps): upgrade React to version 18\n# chore(deps): upgrade React to version 18\n